# mobie 0.0.1

- First GitHub-ready version.
- App code kept in one `inst/app/app.R` file to stay close to the original script.
- Added `run_mobie()` helper.
- Added explicit `library(fmesher)` loading.
